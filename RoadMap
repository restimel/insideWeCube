Legend:
 * task that should be done for this version
 + task which could be great to do but will be done if enough motivation or in a future version
 ✓ task done in this version
 ✗ dropped task
 ? need more thinking on this task
 → some note or some new important task

V1.3
	General
	* Support cubes with not normal size
		* Allow to create cube with not normal size
	+ accept key shortcut (see how to manage them through menu)
	+ Minification: do a minify version

	Build Cube
	✓ See phantom ball
	✓ Defines phantom in cubes
	✓ Display novice cubes as 5x5x5
	+ auto-save to draft
		→ FAQ: What is draft cube
	* key Shortcut (Ctrl+S / Ctrl+z)
	+ Add not accessible cell for advanced tool

	Cube analyzer
	* allow to find moves with a phantom ball

	Auto-Generator
	upgrade interface to choose levels and options
		+ change start position
		+ allow to rotate levels
		✓ Choose only specific cubes (normal or novice)
	* manage level by level
		* add an interface to tell wich level are allowed on each level
	* add a filter to find good levels only which match the filter
	* Display previous result when coming back to cubeGenerator
	+ interact with manage cube to edit it
	+ Performance
		+ improve speed execution
		+ improve memory consumption
			→ hash usage
		+ improve DOM consumption
			→ limit results
			→ Infinite scroll

/!\ Bugs
	General
	* translation is not done for compatibility check message

	About
	* Dates are always in English

	Analyze Cube
	+ Skinning of the instructions to the way back

──────────────────────────────────────────────────────
V2.0 (or later... if enough motivation)

	General
	+ Add tests (why I haven't start with that :'( )
	+ Rebuilt interface by using a framework (like VueJS)

	Build cube
	? switch level (need idea about design)

	Analyze Cube
	✗ (bonus, if I really want) identify in which cube we are if no positions match

	Cube3D
	✗ (I don't think I'll do it) fallback rendering for Browser which doesn't support RotateX